var mainController = {	countPerPage: 50,	currentPage: 1,	totalPage: 1,	data: null,	content:"",	category:"all...",	country:"all...",	woeidMap:null,	countryName:[],		_init : function(){		this._connectEvent();	},		_connectEvent : function(){		console.debug("connect events");		$("#search_button").click(mainController.handleSearch);	},		handleSearch: function(){		mainController.content = $("#content_input").val();		mainModel.search(mainController.content, mainController.searchOnSuccess, mainController.searchOnError);	},			searchOnSuccess: function(data){		console.debug(data);		$("#keywords_result").val(data);	},		searchOnError: function(xhr_data){		mainController.popup("Erorr Occurs");	},		popup: function(message) {			// get the screen height and width  	var maskHeight = $(document).height();  	var maskWidth = $(window).width();	var windowHeight = $(window).height();		console.debug(maskHeight);	console.debug(windowHeight);		// calculate the values for center alignment	var dialogTop =  (windowHeight/2)- ($('#dialog-box').height())/2;  	var dialogLeft = (maskWidth/2) - ($('#dialog-box').width()/2); 		// assign values to the overlay and dialog box	$('#dialog-overlay').css({height:maskHeight, width:maskWidth}).show();	$('#dialog-box').css({top:dialogTop, left:dialogLeft}).show();	// display the message	$('#dialog-message').html(message);			}};$(function() {	mainController._init();});